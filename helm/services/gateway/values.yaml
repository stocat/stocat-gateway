# Default overrides for the Auth API deployment.
# Update image/tag/ports per release and layer environment-specific files on top.

nameOverride: gateway

global:
  labels:
    app.kubernetes.io/part-of: stocat
    app.kubernetes.io/component: gateway

image:
  repository: stocat/gateway
  tag: "0.0.1"

containerPort: 8080

service:
  enabled: true
  port: 8080

env: []

profiles:
  active: ""
